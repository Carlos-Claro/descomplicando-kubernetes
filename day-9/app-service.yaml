apiVersion: v1
kind: Service
metadata:
  name: giropops-senhas
  labels:
    app: giropops-senhas
spec:
  selector:
    app: giropops-senhas
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      name: tcp-app
  type: ClusterIP
  # internalTrafficPolicy: Local
  # externalIPs:
  #   - 192.168.0.106
  #   - 45.189.176.201
