apiVersion: v1
kind: Pod
metadata:
  labels:
    run: server
    service: webserver
  name: server
spec:
  containers:
  - image: nginx
    name: server
    resources: {}
  - image: busybox 
    name: busybox
    args: 
    - sleep
    - "60"
  dnsPolicy: ClusterFirst
  restartPolicy: Always
